{
    "configurations": [
        {
            "name": "Containers: Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/.tools/package/package.json",
                "localRoot": "${workspaceFolder}/.tools/package"
            }
        }
    ]
}